# We need to write configurations so that it connects to the Mysql
# These are standard configurations to connect with DB

# Mandatory Step -> Always create the database

# Write these properties as it is to make connection with sql

spring.datasource.url=jdbc:mysql://localhost:3306/library?createTableIfNotExists=true

# Set your configuration
spring.datasource.username=root
spring.datasource.password=S@vew0rld

# We are writing this thing for hibernate
spring.jpa.hibernate.ddl-auto=update
# It means if the Entity has been modified --> Then update the Sql table also


server.port=9999
